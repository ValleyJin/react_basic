spring:
  application:
    name: lesson-09-spring-menu-crud
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5434/beanlog_lesson09}
    username: ${SPRING_DATASOURCE_USERNAME:beanlog}
    password: ${SPRING_DATASOURCE_PASSWORD:beanlog}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: ${SERVER_PORT:8080}

app:
  jwt:
    secret: ${APP_JWT_SECRET:beanlog-dev-only-change-me-use-32chars-minimum-for-hs256!!}
    expiration-ms: ${APP_JWT_EXPIRATION_MS:86400000}
